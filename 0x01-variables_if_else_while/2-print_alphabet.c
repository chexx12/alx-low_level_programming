#include <stdio.h>

/**
* main - assign a random number to the variable
* Return: 0 if exited properly, non-zero otherwise
*/

int main(void)
{
	char ch;

	for (ch = 'a'; ch <= 'z'; ch++)
		putchar(ch);


	putchar('\n');

	return (0);
}
